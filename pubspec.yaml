name: uber_salud
description: A new Flutter project for Home Health Services.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Core Logic
  http: ^1.2.0
  provider: ^6.1.1
  supabase_flutter: ^2.3.0
  
  # UI & Mapping
  flutter_map: ^6.1.0
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
